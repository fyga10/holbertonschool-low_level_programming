#include "main.h"
/**
 *_isalpha - check character
 * @c: check ascii
 *
 *Return: 0 or 1
 */

int main(void)
{
  if (((c >= 'a') && (c <= 'z')) || ((c >= 'A') && (c <= 'Z')))
{
return (1);
}
else
{
return (0);
}
}
