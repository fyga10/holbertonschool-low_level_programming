#include "main.h"
#include <stdio.h>

/**
 *_abs(int) - absolute value
 *@r: number
 *Return: always 0
 */

int _abs(int r)
{
int r;
int c;
if (r < 0)
{
c = (r*(-1));
_putchar('c');
}
else
{
c= r;
_putchar('c');
}
return(c);
}
