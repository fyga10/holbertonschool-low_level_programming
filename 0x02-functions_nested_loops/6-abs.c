#include "main.h"
#include <stdio.h>

/**
 *_abs(int) - absolute value
 *
 *Return: always 0
 */

int _abs(int)
{
int r;
int c;
if (r < 0)
{
c = (r*(-1));
_putchar('c');
}
else (r >= 0)
{
c= r;
_putchar('c');
}
return(c);
}
