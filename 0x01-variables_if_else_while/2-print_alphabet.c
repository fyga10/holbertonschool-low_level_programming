#include <stdio.h>

/**
 * main - prints alphabet
 *
 * return: always 0 (success)
 */

int main(void)

{
int c = 97;
 while (a <= 122)
   {
     putchar(a);
     a++;
   }
 putchar('\n')
   return (0);
}
